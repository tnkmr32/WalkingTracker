# 画面設計：記録詳細画面

参照：[画面一覧・遷移](./overview.md)

---

## 概要

| 項目     | 内容                                                             |
| -------- | ---------------------------------------------------------------- |
| 画面ID   | HIS002                                                           |
| クラス名 | `SessionDetailScreen`                                            |
| 遷移元   | [HIS001 記録一覧画面](./HIS001-history.md)（セッションをタップ） |
| 目的     | 特定の歩行セッションの詳細と軌跡を表示する                       |

---

## 画面間パラメータ一覧

### 受信パラメータ

| パラメータ名 | 型     | 説明                   | 送信元              |
| ------------ | ------ | ---------------------- | ------------------- |
| `sessionId`  | `Long` | 表示するセッションのID | HIS001 記録一覧画面 |

### 送信パラメータ

なし

---

## 画面項目一覧

| 項目名                       | 種別             | 取得元                                                                              |
| ---------------------------- | ---------------- | ----------------------------------------------------------------------------------- |
| タイトル（日付）             | テキスト         | Room `WalkSession.started_at`（`sessionId` で取得、yyyy/MM/dd（E） にフォーマット） |
| 地図エリア（セッション軌跡） | 地図（OSMDroid） | Room `TrackPoint`（`session_id = sessionId` でフィルタ）                            |
| 歩数                         | テキスト         | Room `WalkSession.steps`（`sessionId` で取得）                                      |
| 距離                         | テキスト         | Room `WalkSession.distance_meters`（`sessionId` で取得）                            |
| 所要時間                     | テキスト         | Room `WalkSession.finished_at` − `started_at`（`sessionId` で取得）                 |
| 開始時刻                     | テキスト         | Room `WalkSession.started_at`（HH:mm にフォーマット）                               |
| 終了時刻                     | テキスト         | Room `WalkSession.finished_at`（HH:mm にフォーマット）                              |

---

## ワイヤーフレーム

```
┌─────────────────────────────┐
│ ←  2026/06/27（金）          │
│─────────────────────────────│
│  [地図エリア (OSMDroid)]     │
│  ・このセッションの軌跡（赤）│
│                             │
│─────────────────────────────│
│  歩数      12,345 歩         │
│  距離       8.7 km           │
│  所要時間   45 分            │
│  開始時刻   09:12            │
│  終了時刻   09:57            │
└─────────────────────────────┘
```

---

## デザインカンプ

> 下記のリンク先のファイルをブラウザで開いてください。

**[HIS002-session-detail.html](./design-comp/HIS002-session-detail.html)**

---

## 状態と動作

| 状態       | 動作                                                           |
| ---------- | -------------------------------------------------------------- |
| 初回読み込み中 | TrackPoint 取得・地図読み込み中は、画面中央に `CircularProgressIndicator` を表示する。取得完了後にセッション詳細コンテンツへ切り替える（参照：[motion.md](../design-system/motion.md)） |
| 通常       | 指定セッションの TrackPoint を取得して地図上にポリラインを描画 |
| GPS 未記録 | 地図エリアを非表示にして「GPS データなし」を表示               |
| DB読み込みエラー（指摘#8対応） | TrackPoint（またはWalkSession）の取得に失敗した場合、「データの読み込みに失敗しました。再度お試しください」を表示し、再読み込みボタンを提供する（参照：[error-handling.md](../../02-architecture-design/error-handling.md)） |
| 該当セッションなし（指摘#10対応） | `sessionId` に一致する `WalkSession` が存在しない場合（不正パラメータ・削除済み等）、地図・数値項目は表示せず「このセッションは見つかりませんでした」を表示し、[HIS001 記録一覧画面](./HIS001-history.md) へ戻るボタンを提供する（DB読み込みエラーとは異なりクエリ自体は成功しているため、再読み込みボタンではなく一覧へ戻るボタンのみとする） |
| Backボタン | [HIS001 記録一覧画面](./HIS001-history.md) へ戻る              |
