# 外部設計レビュー観点

参照：[基本設計](./basic-design.md)

> レビュー実施日：2026-06-28　対象ブランチ：feature/add-basic-design-specs

---

## 1. 要件との整合性

### 1.1 機能要件の網羅

- [x] 要件定義書に記載された全機能が、いずれかの画面・業務フローに反映されているか
- [ ] 要件定義書にある機能で、画面設計・業務設計に未記載のものがないか　→ **指摘#4**
- [x] 設計書に追加された機能・仕様変更が要件定義書と矛盾していないか

### 1.2 非機能要件への言及

- [ ] パフォーマンス要件（地図描画速度、GPS 取得間隔など）が設計に反映されているか　→ **指摘#5**
- [ ] プライバシー・セキュリティ要件（位置情報の扱い、ローカル保存方針）に言及があるか　→ **指摘#17**
- [x] オフライン動作の方針が明示されているか

---

## 2. 画面設計

### 2.1 画面一覧・画面遷移

- [x] 画面一覧（[screens/README.md](./screens/README.md)）に漏れがないか
- [x] 画面遷移図（Mermaid）がすべての遷移パターンを網羅しているか
- [x] エラー発生時・権限拒否時の遷移先が定義されているか
- [ ] 「戻る（Back）」操作の挙動がすべての画面で定義されているか　→ **指摘#6**
- [ ] BottomNavigationBar の状態管理（タブ間移動時のスタック）が明確か　→ **指摘#18**

### 2.2 各画面の項目定義

- [x] 全画面で「画面項目一覧」が定義されているか
- [x] 各項目の取得元（データソース）が明示されているか
- [x] 項目の表示フォーマット（日付・時刻・数値の書式）が統一されているか
- [ ] ページネーション・スクロールの挙動が必要な画面で定義されているか（HIS001 など）　→ **指摘#19**

### 2.3 状態・動作の定義

- [x] 各画面で「状態と動作」テーブルが定義されているか
- [ ] ローディング状態の表示が定義されているか　→ **指摘#7**
- [x] データ0件時の表示（エンプティステート）が全画面で定義されているか
- [ ] エラー状態（通信失敗、DB エラー等）の表示が定義されているか　→ **指摘#8**
- [ ] 画面の業務処理で使用する DB への CRUD 操作やアセット取得について記載されているか　→ **指摘#9**

### 2.4 画面間パラメータ

- [x] 全画面で受信・送信パラメータが定義されているか
- [x] パラメータの型と値域が明示されているか
- [ ] 存在しないIDを受け取った場合（不正パラメータ）の挙動が定義されているか　→ **指摘#10**

### 2.5 デザインの一貫性

- [x] デザインカンプがデザインシステム（[design-system/](./design-system/README.md)）のトークンに準拠しているか
- [x] コンポーネントの使い方がデザインシステムの定義と一致しているか（[components.md](./design-system/components.md)）
- [x] タイポグラフィ・カラー・スペーシングがトークン経由で統一されているか

---

## 3. 業務設計

### 3.1 業務フローの網羅性

- [ ] 正常系フローだけでなく、例外・エラー系の分岐が定義されているか　→ **指摘#11**
- [x] フロー内の各処理ステップに曖昧さがないか（実装者が迷わない粒度か）
- [x] フローの開始条件・終了条件が明示されているか

### 3.2 業務間の連携

- [ ] WLK → TRV へのデータ受け渡し（TrackPoint の提供タイミング・インターフェース）が明確か　→ **指摘#12**
- [x] WLK → HIS へのデータ保存（WalkSession 確定のタイミング）が明確か
- [ ] SET（パーミッション） が未取得の状態で WLK / TRV / HIS を開こうとした場合の動作が定義されているか　→ **指摘#13**

### 3.3 TRV：踏破判定ロジック

- [x] 50m のマッチング閾値の根拠が記録されているか
- [x] 同一セグメントへの重複登録防止の条件が明確か
- [ ] GeoJSON の読み込み失敗時の挙動が定義されているか　→ **指摘#20**
- [ ] 踏破進捗率の計算式（距離ベース）が明示されているか　→ **指摘#1**

### 3.4 WLK：記録フロー

- [x] ForegroundService の起動・停止条件が明確か
- [ ] アプリを強制終了した場合（記録が途中で切れた場合）の WalkSession の扱いが定義されているか　→ **指摘#2**
- [x] GPS 取得間隔（5秒）が要件・バッテリー消費の観点で妥当か

---

## 4. データ設計

### 4.1 テーブル定義の完全性

- [x] 全テーブルに主キー（PK）が定義されているか
- [x] 外部キー（FK）が必要な箇所に定義されているか（例：`TrackPoint.session_id → WalkSession.id`）
- [x] NULL 許容・NOT NULL 制約が各カラムで明示されているか
- [x] インデックスが必要なカラムに定義されているか（例：`session_id`、`started_at`）

### 4.2 データ型・値域

- [x] 緯度・経度のデータ型と精度が適切か（REAL / DOUBLE の選択）
- [x] 日時カラムの型（UNIX タイムスタンプ or ISO8601 等）と精度が統一されているか
- [ ] 距離・歩数カラムの型と想定最大値が妥当か　→ **指摘#21**

### 4.3 GeoJSON アセット

- [ ] GeoJSON のプロパティ定義（[asset/highway-geojson.md](./asset/highway-geojson.md)）が TRV の判定ロジックと一致しているか　→ **指摘#3**
- [ ] セグメント数・ファイルサイズの見積もりがあるか　→ **指摘#14**
- [ ] GeoJSON の更新手順が定義されているか　→ **指摘#22**

### 4.4 AppSettings

- [x] シングルトン制約（`id = 1` のみ）の保証手段が定義されているか
- [x] 初期値の定義があるか

---

## 5. パーミッション設計

- [x] アプリが必要とする全パーミッションがリストアップされているか
- [x] 各パーミッションの用途・取得タイミングが明示されているか
- [x] 「一度拒否」と「二度と表示しない」の挙動が区別されているか（SET001 のフロー）
- [x] `ACTIVITY_RECOGNITION`（歩数）と `ACCESS_FINE_LOCATION`（GPS）の両方の扱いが定義されているか
- [ ] パーミッション取得後に設定が変更（ユーザーが後から剥奪）された場合の挙動が定義されているか　→ **指摘#15**

---

## 6. エラーハンドリング・ユーザーフィードバック

- [x] [error-handling.md](../02-architecture-design/error-handling.md) の方針が各業務フロー・画面設計に反映されているか
- [x] エラー時のユーザー向けメッセージ・UI（Snackbar、ダイアログ等）が定義されているか
- [ ] リトライ可能なエラーとリトライ不可のエラーが区別されているか　→ **指摘#23**
- [x] GPS 信号が弱い・歩数センサーが利用不可の場合の挙動が全画面で定義されているか

---

## 7. バックグラウンド動作

- [x] ForegroundService の仕様が [background-tracking.md](../02-architecture-design/background-tracking.md) に定義されており、画面設計（WLK001）と整合しているか
- [x] 常駐通知の表示内容・タップ時の動作が定義されているか
- [ ] 他アプリに切り替えた場合・画面ロック時の動作が定義されているか　→ **指摘#24**
- [ ] Android のバッテリー最適化（Doze モード）への対処が検討されているか　→ **指摘#25**

---

## 8. ドキュメント品質

### 8.1 整合性・一貫性

- [x] 画面 ID・クラス名がすべてのドキュメント間で統一されているか
- [ ] 相互参照リンク（`参照：` 行）が正しいパスを指しているか（リンク切れがないか）　→ **指摘#16**
- [x] 業務フロー・画面遷移・画面仕様で同じ概念に異なる用語が使われていないか

### 8.2 記載の粒度

- [x] ワイヤーフレームとデザインカンプの内容が一致しているか
- [x] 「未定」「TBD」などの未記入箇所がないか（あれば課題として明示されているか）
- [x] 実装者が判断に迷わない粒度で仕様が記載されているか

---

## レビュー結果記録

### 凡例

| 重要度 | 基準 |
|--------|------|
| 高 | 実装が不可能・仕様が破綻する。設計を修正しないと進められない |
| 中 | 実装できるが品質・保守性・ユーザー体験に影響する |
| 低 | あった方が丁寧だが、なくても実装に支障はない |

### 指摘一覧

| # | 観点カテゴリ | 指摘内容 | 重要度 | 対応者 | ステータス |
|---|------------|---------|--------|--------|-----------|
| 1 | 3.3 TRV ロジック | **【重大】** GeoJSON の Feature.properties にセグメント個別の距離フィールド（`distance_meters` 等）が定義されていない。TRV001 の「踏破済み距離」表示や進捗率計算に必要なデータソースが存在しない | 高 | | 完了 |
| 2 | 3.4 WLK フロー | **【重大】** アプリ強制終了時（`finished_at = NULL` のままのセッション）の扱いが未定義。次回起動時に未完了セッションを検出した場合の処理（破棄・自動完了など）が必要 | 高 | | 完了 |
| 3 | 4.3 GeoJSON | **【重大】** 指摘#1 と同根。Feature.properties に `segment_index` しか定義がなく、各セグメントの距離（メートル）フィールドがない。踏破済み距離の集計に必要なため追加が必要 | 高 | | 完了 |
| 4 | 1.1 機能要件の網羅 | 追加機能（現在地コンテキスト情報・歩行サマリー・休息日表現）が設計書に存在しない。MVP スコープ外であれば「スコープ外のため対象外」と明記が必要 | 中 | | 完了 |
| 5 | 1.2 非機能要件 | 「地図・軌跡の描画が2秒以内」のパフォーマンス要件が画面設計・データ設計に反映されていない（インデックス・データ量の考慮が必要） | 中 | | 未対応 |
| 6 | 2.1 画面遷移 | SET001 でのバックキー操作の挙動が未定義（アプリを終了するか、再確認ダイアログを表示するか） | 中 | | 未対応 |
| 7 | 2.3 状態・動作 | 全画面でローディング状態（DB取得中・地図読み込み中）の UI 表示が未定義 | 中 | | 未対応 |
| 8 | 2.3 状態・動作 | DB 読み込みエラーは error-handling.md に定義されているが、HIS001・HIS002・TRV001 の各画面仕様書に記載がない（画面仕様に転記が必要） | 中 | | 未対応 |
| 9 | 2.3 状態・動作 | WLK001 の画面仕様書に TrackPoint の INSERT・WalkSession の INSERT/UPDATE などの書き込み操作の記載がない（取得元は定義されているが書き込み操作が不明） | 中 | | 未対応 |
| 10 | 2.4 画面間パラメータ | HIS002 で存在しない `sessionId` を受け取った場合（不正パラメータ・削除済みセッション）の挙動が未定義 | 中 | | 未対応 |
| 11 | 3.1 業務フロー | WLK 業務フローに GeoJSON 読み込み失敗時の分岐がない（TRV への TrackPoint 提供処理が失敗した場合） | 中 | | 未対応 |
| 12 | 3.2 業務間連携 | WLK → TRV のデータ受け渡しの具体的な実装インターフェースが不明（ForegroundService 内で直接呼ぶのか、Flow/Channel 経由か）。実装者が迷う可能性がある | 中 | | 未対応 |
| 13 | 3.2 業務間連携 | パーミッションがアプリ使用中に剥奪された場合の WLK/TRV/HIS の動作が未定義。記録中に GPS 権限が剥奪されるケースは実際に発生しうる | 中 | | 未対応 |
| 14 | 4.3 GeoJSON | セグメント数・GeoJSON ファイルサイズの見積もりがなく、アプリバンドルサイズや起動時メモリへの影響が評価できない | 中 | | 未対応 |
| 15 | 5. パーミッション | 記録中に GPS・歩数センサーパーミッションをユーザーが設定アプリから剥奪した場合の ForegroundService・UI の挙動が未定義 | 中 | | 未対応 |
| 16 | 8.1 ドキュメント品質 | **【リンク切れ複数】** ①全画面仕様書の `参照：[画面一覧・遷移](./overview.md)` → 正しくは `./README.md` ②permissions.md の `参照：[screens/permission.md]` → 正しくは `screens/SET001-permission.md` ③全テーブル定義書の `参照：[テーブル一覧](./table-list.md)` → 正しくは `./README.md` | 中 | | 未対応 |
| 17 | 1.2 非機能要件 | 「位置情報を外部サーバーへ送信しない」のプライバシー要件が外部設計書に明示されていない（アーキテクチャ方針として記載が必要） | 低 | | 未対応 |
| 18 | 2.1 画面遷移 | BottomNavigationBar のバックスタック管理が未定義（タブ間移動後にバックキーを押した場合の挙動） | 低 | | 未対応 |
| 19 | 2.2 項目定義 | HIS001 の一覧スクロールにおいてページング・遅延読み込みの方針が未定義（大量データ時の挙動） | 低 | | 未対応 |
| 20 | 3.3 TRV ロジック | GeoJSON 読み込み失敗時の挙動は error-handling.md に記載あるが、TRV 業務フロー（Mermaid）に分岐として記載されていない | 低 | | 未対応 |
| 21 | 4.2 データ型 | `WalkSession.distance_meters` が Float 型。長距離の旧街道（例：東海道 500,000m）を複数セッションで累計すると精度劣化が生じる可能性がある。Double への変更を検討 | 低 | | 未対応 |
| 22 | 4.3 GeoJSON | GeoJSON の更新手順（新しい旧街道の追加や座標修正のワークフロー）が未記載 | 低 | | 未対応 |
| 23 | 6. エラーハンドリング | error-handling.md のエラー対応に「リトライ可能（手動）」「自動リトライ」「リトライ不可」の明示的な分類がなく、実装者の判断が分かれる可能性がある | 低 | | 未対応 |
| 24 | 7. バックグラウンド | 画面ロック時の動作について background-tracking.md・WLK001 仕様書いずれにも明示的な記載がない（ForegroundService は継続するはずだが確認が必要） | 低 | | 未対応 |
| 25 | 7. バックグラウンド | Android の Doze モード・App Standby への対処方針が未記載。ForegroundService は保護されるが、方針として記録しておくことを推奨 | 低 | | 未対応 |
